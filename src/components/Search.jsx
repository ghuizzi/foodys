import React from "react";

const Search = () => {
  return (
    <div>
      <input
        type="text"
        placeholder="Buscar artistas, bandas, pistas, podscast"
      />
      <button>Buscar</button>
    </div>
  );
};

export default Search;
